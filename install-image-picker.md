# 📸 Installing Image Picker

Before we can test avatar uploads, you need to install the image picker package:

```bash
cd fretiko-mobile
npx expo install expo-image-picker
```

This allows users to:
- Take photos with camera
- Pick images from photo library
- Upload as profile avatars

After installing, restart your Expo development server:
```bash
npm start
```